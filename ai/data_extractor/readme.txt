NOTE: Had to manually edit a lot of the source data files by hand since some of the Q and A weren't 
      structured consistently so the JSON files were screwed up. I think the data is aligned now.
      There might be some weird entries here so watch out.

NOTE 2: You can use main.py on any directory and if it has .txt with QnA files ordered like

Q:

Questions

A:

Answers

Then it should be able to create a data.json from that too.

NOTE 3: Python 3.12.12